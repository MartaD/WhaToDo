<!-- 

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.person_id %></td>
    <td><%= task.duration %></td>
    <td><%= link_to 'Show', task %></td>
    <td><%= link_to 'Edit', edit_task_path(task) %></td>
    <td><%= link_to 'Destroy', task, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %> -->

<div id='wrap'>

<div id='external-events'>
<h4>Tasks List</h4>
<% @tasks.each do |task| %>
  <div class='external-event'><%= link_to task.name, task %> | <%= link_to 'Edit', edit_task_path(task) %> | <%= link_to 'Destroy', task, confirm: 'Are you sure?', method: :delete %></div>
<% end %>

<%= link_to 'New Task', new_task_path %>
</div>

<div id='calendar'></div>

<div style='clear:both'></div>
</div>
